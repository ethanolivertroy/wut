name: CI

on:
  pull_request:
    paths:
      - "**/*.rs"
      - Cargo.toml
      - Cargo.lock
      - README.md
      - LICENSE
      - "assets/**"
      - install.sh
      - "tests/**"
      - ".github/workflows/**"
  push:
    branches: [main]
    paths:
      - "**/*.rs"
      - Cargo.toml
      - Cargo.lock
      - README.md
      - LICENSE
      - "assets/**"
      - install.sh
      - "tests/**"
      - ".github/workflows/**"

permissions:
  contents: read

jobs:
  check:
    name: Check (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-24.04, macos-15]

    steps:
      - uses: actions/checkout@v6
      - name: Install shell test tools
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install --yes shellcheck zsh
      - name: Install stable Rust
        run: |
          rustup update stable
          rustup default stable
          rustup component add clippy rustfmt
      - run: cargo fmt --check
      - run: cargo test --locked
      - name: Verify provider-environment test isolation
        env:
          OPENCODE_CONFIG_CONTENT: not-json
        run: cargo test --locked
      - run: cargo clippy --locked --all-targets -- -D warnings
      - name: Verify product identity
        run: sh tests/rebrand.sh
      - name: Test installer
        run: sh tests/install.sh
      - name: Lint installer
        if: runner.os == 'Linux'
        run: shellcheck install.sh tests/install.sh tests/rebrand.sh

  msrv:
    name: Rust 1.88 minimum
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v6
      - run: rustup toolchain install 1.88.0 --profile minimal
      - run: cargo +1.88.0 test --locked
